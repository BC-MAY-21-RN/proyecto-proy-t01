import styled from 'styled-components/native';
import colors from '../../constants/colors';

export const TextLinkCustom = styled.Text`
  color: ${colors.red};
  margin-top: 10px;
  align-self: center;
  font-size: 15px;
`;

export const TextLinkTouchableOpacity = styled.TouchableOpacity``;
